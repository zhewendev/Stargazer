// Thin pageBody wrapper for the Home pageType.
// The actual UI lives in src/components/Home.tsx so it lives outside `quartz/`
// and survives framework updates.

import HomePage from "../../../src/components/Home"
export default HomePage
